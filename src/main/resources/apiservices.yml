apiservice:
  home:
    baseurl: http://ipaddr:port/resource
    get: /feed/**
    post: /feed/**

  # baseurl(API 서버주소): http://ipaddr:port
  # method(get,post,put,del): /{service name}/** (URI 경로)
  news:
    baseurl: http://172.20.92.214:31005
    get: /news/**

  match:
    baseurl: http://localhost:9000/match
    get: /match/**

  rank:
    baseurl: http://localhost:9000/rank
    get: /rank/**
